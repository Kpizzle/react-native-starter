import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const CounterScreen = () => {
  return <Text>Current Count: </Text>;
};

const styles = StyleSheet.create({});

export default CounterScreen;